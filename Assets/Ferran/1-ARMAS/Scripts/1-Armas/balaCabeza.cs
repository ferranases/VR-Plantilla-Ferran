using UnityEngine;

public class balaCabeza : MonoBehaviour
{
    //private void OnCollisionEnter(Collision collision)
    //{
    //    Debug.Log("CollisionName: " + collision.gameObject.name);


    //    Vector3 explosionPos = collision.contacts[0].point;
    //    Collider[] colliders = Physics.OverlapSphere(explosionPos, 1);
    //    foreach (Collider hit in colliders)
    //    {
    //        Rigidbody rb = hit.GetComponent<Rigidbody>();

    //        if (rb != null)
    //            rb.AddExplosionForce(5, explosionPos, 1, 3.0F);
    //    }

    //    Destroy(gameObject, 1);
    //}

}
